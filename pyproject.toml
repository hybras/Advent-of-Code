[project]
name = "aoc-2024"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aocp>=1.0.0",
]

[dependency-groups]
dev = [
    "notebook>=7.3.1",
]
